
1. `mkdir certs`
2. Generate csr using wolfTPM `csr.exe`
3. Process csr to generate certificate `client-rsa-cert.pem`
4. Open solution `tpm-examples/tpm-examples.sln`
5. Set host `MQTT_HOST` in `mqtt_tpm_simple.c`
6. For `curl_tpm`, copy server CA cert to ca-cert.pem or change path setting `CURLOPT_CAINFO`
7. Build solution

After successfully building the example is ready to be run.